plugins {
    id("com.mkobit.jenkins.pipelines.shared-library") version "0.6.2"
}

group 'jenkins.sample'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
